# geo-time-getter-example

install: 

```
$ yarn
$ cp src/config.sample.json src/config.json
$ nano src/config.json
```

run:

```
yarn start
```

develop: 

```
yarn remove geo-time-getter && yarn cache clean && yarn add file:./../geo-time-getter && yarn start
```
